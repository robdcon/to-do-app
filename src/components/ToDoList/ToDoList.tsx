import React, { useState } from 'react';
import PropTypes from 'prop-types';
import ToDo from '../ToDo'
//import { Test } from './ToDoList.styles';



// const ToDoList: React.FC<ToDo> = () => {

 

//  const toggleDone: ToggleDone = (targetToDo) => {

//   console.log('done');

//  // const updatedToDos = toDoList.map((toDo) => {
//     if(toDo === targetToDo){
//       return {
//         ...toDo,
//         done: !toDo.done
//       }
//     }
//     return toDo;
//   })

//   setToDoList(updatedToDos)

// }

//   return (

//   <ul className="ToDoListWrapper">
//    { 
//       toDoList.map((todo, i) => {
//        return <ToDo key={i} text={todo.text} done={todo.done} id={todo.id} toggleDone={todo.toggleDone}/>
//       })
//     }
//   </ul>

//   )

// };

// ToDoList.propTypes = {
//   // bla: PropTypes.string,
// };

// ToDoList.defaultProps = {
//   // bla: 'test',
// };

// export default ToDoList;
